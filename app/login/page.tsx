import { HStack, Button } from '@chakra-ui/react';

const LoginPage = () => {
  return (
    <HStack>
      <Button>Click me</Button>
      <Button>Click me</Button>
    </HStack>
  );
};
export default LoginPage;
